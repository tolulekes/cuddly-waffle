package com.toluleke.mktfy.ui.screens.loggedOut.signUp

import com.toluleke.mktfy.core.ActionViewModel

class SignUpViewModel : ActionViewModel<SignUpViewState, SignUpAction>(SignUpViewState()) {
    override fun collectAction(action: SignUpAction) {
        when (action) {
        }
    }
}

