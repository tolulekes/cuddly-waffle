package com.toluleke.mktfy.ui.screens.loggedIn.notification

import com.toluleke.mktfy.core.ActionViewModel

class NotificationViewModel: ActionViewModel<NotificationViewState, NotificationAction>(NotificationViewState()) {
    override fun collectAction(action: NotificationAction) {
        when (action) {
        }
    }
}

