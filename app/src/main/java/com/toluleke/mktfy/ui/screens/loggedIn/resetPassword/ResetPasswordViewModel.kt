package com.toluleke.mktfy.ui.screens.loggedIn.resetPassword

import com.toluleke.mktfy.core.ActionViewModel

class ResetPasswordViewModel : ActionViewModel<ResetPasswordViewState, ResetPasswordAction>(ResetPasswordViewState()) {
    override fun collectAction(action: ResetPasswordAction) {
        when (action) {
        }
    }
}

