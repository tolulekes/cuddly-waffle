package com.toluleke.mktfy.ui.screens.loggedOut.forgetPassword

import com.toluleke.mktfy.core.ActionViewModel

class ForgetPasswordViewModel : ActionViewModel<ForgetPasswordViewState, ForgetPasswordAction>(ForgetPasswordViewState()) {
    override fun collectAction(action: ForgetPasswordAction) {
        when (action) {
        }
    }
}

