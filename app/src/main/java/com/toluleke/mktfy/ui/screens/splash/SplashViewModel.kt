package com.toluleke.mktfy.ui.screens.splash

import com.toluleke.mktfy.core.ActionViewModel

class SplashViewModel : ActionViewModel<SplashViewState, SplashAction>(SplashViewState()) {
    override fun collectAction(action: SplashAction) {
        when (action) {
        }
    }
}

