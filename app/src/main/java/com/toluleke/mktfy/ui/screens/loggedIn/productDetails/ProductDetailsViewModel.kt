package com.toluleke.mktfy.ui.screens.loggedIn.productDetails

import com.toluleke.mktfy.core.ActionViewModel

class ProductDetailsViewModel : ActionViewModel<ProductDetailsViewState, ProductDetailsAction>(ProductDetailsViewState()) {
    override fun collectAction(action: ProductDetailsAction) {
        when (action) {
        }
    }
}

