package com.toluleke.mktfy.ui.screens.tos

import com.toluleke.mktfy.core.ActionViewModel

class TOSViewModel : ActionViewModel<TOSViewState, TOSAction>(TOSViewState()) {
    override fun collectAction(action: TOSAction) {
        when (action) {
        }
    }
}

