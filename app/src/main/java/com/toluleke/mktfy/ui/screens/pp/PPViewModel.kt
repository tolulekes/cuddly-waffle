package com.toluleke.mktfy.ui.screens.pp

import com.toluleke.mktfy.core.ActionViewModel

class PPViewModel : ActionViewModel<PPViewState, PPAction>(PPViewState()) {
    override fun collectAction(action: PPAction) {
        when (action) {
        }
    }
}

